// 获取 range input 和用于显示值的 span 元素
const interestRange = document.getElementById('interest');
const rangeValueSpan = document.getElementById('range-value');

// TODO: 添加一个 input 事件监听器到 range input 上
// 当滑块的值改变时，更新 span 标签中的文本内容，使其与滑块的当前值保持一致。